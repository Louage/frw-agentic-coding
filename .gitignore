node_modules/
dist/
out/
out-test/
*.vsix
.vscode-test/
*.log

# Scratch/regenerable working area: sync caches, Extension Development Host
# profiles, tool dumps. Everything under tmp/ is disposable except the files
# explicitly re-included below.
tmp/*
!tmp/agentic-coding-al-TrainingMaterial.md

# Claude Code agent worktrees (isolated checkouts created per task, never committed)
.claude/worktrees/
